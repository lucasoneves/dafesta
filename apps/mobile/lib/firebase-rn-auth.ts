// @ts-ignore - getReactNativePersistence exists at runtime on React Native
// The Firebase SDK uses conditional exports and TypeScript resolves the
// wrong entry point. The React Native bundler resolves it correctly.
export { getReactNativePersistence } from "firebase/auth";
